# java
Java projects 

1) Given the size of the chess board and initial position of the knight, what is the probability that after k moves the knight will be inside the chess board.

Note:-
b) The knight makes its all 8 possible moves with equal probability.
a) Once the knight is outside the chess board it cannot come back inside.


2) Given a string containing just the characters ‘(‘, ‘)’, ‘{‘, ‘}’, ‘[' and ']‘, determine if the input string is valid.

The brackets must close in the correct order, “()” and “()[]{}” are all valid but “(]” and “([)]” are not.
